/ = link_to "Already read it", book_readers_path(@book), remote: true, method: :post, class: "btn btn-primary"
= form_for :reader, { url: book_readers_path(@book), remote: true } do |f|
  = f.submit "Already read it", class: "btn btn-primary"