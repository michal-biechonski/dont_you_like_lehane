/ = link_to "Add to wishlist", book_future_readers_path(@book), remote: true, method: :post, class: "button button--confirm"
= form_for :reader, { url: book_future_readers_path(@book), remote: true } do |f|
  = f.submit "Add to wishlist", class: "button button--confirm"