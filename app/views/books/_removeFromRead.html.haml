= link_to "Didn't read it", book_reader_path(@book, current_user), remote: true, method: :delete, class: "btn btn-primary"
/ = form_for :reader, { url: book_reader_path(@book, current_user), remote: true } do |f|
/   = f.submit "Didn't read it", class: "btn btn-primary"