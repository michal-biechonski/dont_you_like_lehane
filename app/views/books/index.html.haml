%h1.center Books
- @books.each do |book|
  .row
    .col-md-8.col-md-offset-2
      %a.booksLinks{href:"/books/#{book.id}"}
        .row.well
          .col-md-3.center
            = image_tag book.picture.url(:thumb), class: "bookImage"
          .col-md-9
            .bookContent
              %strong Title:
              = book.title
            .bookContent
              %strong Published at:
              = book.published_at.strftime("%B, %Y")
