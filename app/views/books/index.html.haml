%h1.center Books
- @books.each do |book|
  %main.row
    %article.col-sm-8.col-sm-offset-2
      %a.booksLinks{href:"/books/#{book.id}"}
        .row.well
          %figure.col-sm-3.center
            = image_tag book.picture.url(:thumb), class: "book"
          %section.col-sm-9
            .bookContent
              %strong Title:
              = book.title
            .bookContent
              %strong Published at:
              = book.published_at.strftime("%B, %Y")
