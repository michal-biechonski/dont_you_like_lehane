%h1.center Add comment
%section
  .form
    = form_for [@book, @comment] do |f|

      = render "shared/errors", obj: @comment

      .form__inputs
        = hidden_field_tag("book_id", params[:book_id])
        = f.label :content
        = f.text_area :content, class: "form__input", rows: 6

      .form__inputs
        = f.submit "Add", class: "button button_confirm button_submit"