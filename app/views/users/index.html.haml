%h1.center Users
.row.center.usersRow
  - @users.each do |user|
    .col-lg-3.col-md-4.col-sm-6
      .well.usersWell
        %a{href:"users/#{user.id}"}
          .row.center
            .col-md-4
              = show_avatar(user, 100)
            .col-md-8
              = user.name
